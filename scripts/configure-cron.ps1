[CmdletBinding()]
param()
Write-Host "Daily email now uses the radar worker (08:30 Asia/Shanghai)."
Write-Host "Run scripts/start.ps1 and mybot configure-email. No QwenPaw cron is created."
Write-Host "Existing custom jobs remain unchanged."
